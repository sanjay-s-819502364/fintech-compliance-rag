# Secrets / config
.env
*.pem
*credentials*
.claude/settings.local.json
CLAUDE.local.md

# Regulatory corpus (not redistributed — see SOURCES.md)
corpus/

# Eval run output (contains live kb_id/guardrail_id; regenerate via run_generation.py)
eval/results/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node (lambda/src/ — the Lambda)
node_modules/
.aws-sam/

# Contains live resource IDs (kb_id, etc.) — same reasoning as eval/results/
lambda/samconfig.toml

# OS
.DS_Store
